# Ejemplo de configuración de variables de entorno
# Copia este archivo a .env y rellena con tus datos reales

# Conexión a la BD (Usuario 'app' con permisos limitados)
DATABASE_URL="postgresql://usuario_app:password_seguro@localhost:5432/nombre_bd"

# URL base de la aplicación
NEXT_PUBLIC_APP_URL="http://localhost:3000"